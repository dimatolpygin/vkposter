# STATUS — текущее состояние проекта

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-25 (бутстрап okplan: разбивка на 13 этапов, три слоя прогресса развёрнуты)
**Текущий этап**: Этап 0 — Каркас в Docker (☐ не начат)
**Следующий шаг**: взять этап 0 в работу — `docker-compose.yml` (app + postgres), автомиграции на старте, `/health`, слой логов pino с `request-id`, `node --watch` для автосборки. Критерии — [«Этап 0» в ROADMAP](07_ROADMAP.md#этап-0--каркас-в-docker).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас в Docker | ☐ | `stage-0-done` | — | — |
| 1 | Схема БД + вход в панель | ☐ | `stage-1-done` | — | — |
| 2 | Обнаружение и извлечение статей | ☐ | `stage-2-done` | — | — |
| 3 | Дедуп по URL и по теме | ☐ | `stage-3-done` | — | — |
| 4 | Генерация текста поста (OpenRouter) | ☐ | `stage-4-done` | — | — |
| 5 | Генерация картинки (kie.ai) | ☐ | `stage-5-done` | — | — |
| 6 | Публикация в ВК (postmypost) | ☐ | `stage-6-done` | — | — |
| 7 | Группы ВК в панели | ☐ | `stage-7-done` | — | — |
| 8 | Уникальность по группам + расписание | ☐ | `stage-8-done` | — | — |
| 9 | Автономный cron + ручной режим | ☐ | `stage-9-done` | — | — |
| 10 | Разовое наполнение из архива | ☐ | `stage-10-done` | — | — |
| 11 | Панель наблюдаемости | ☐ | `stage-11-done` | — | — |
| 12 | Деплой на прод + автодеплой | ☐ | `stage-12-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

Нет — этап 0 ещё не взят в работу.
**Ветка**: `dev`

## Известные блокеры

Нет.

## Решения, зафиксированные на бутстрапе

- **Админка — один аккаунт** (админ = клиент), все разделы доступны. Обязательна завершённая
  логика доступа: вход → выход → смена пароля с инвалидацией сессий.
- **Промты хранятся в БД** и правятся в панели с версиями. Сид `post_prompt` — реальный промт
  клиента, копия в `docs/prompts/post_prompt.seed.txt`.
- **Redis не берём**: сервер 2 ядра / 2 ГБ, блокировки прогонов и очередь держим в Postgres
  (advisory locks + таблица задач).
- **Картинки храним у себя** (`/media` volume, отдаём по https домена). Временный URL от kie.ai
  в postmypost не отдаём — истекает до отложенной публикации.
- **Git**: свой репозиторий внутри `vkposter`, ветка `dev`. Папка вложена в чужой репо —
  в родительский корень не коммитим.

## История закрытий

Записывать в обратном хронологическом порядке. Формат:
`YYYY-MM-DD — Этап N: <название> — tag stage-N-done — короткая заметка с конкретикой верификации`.

Пока пусто.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены человеком.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
